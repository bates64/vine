{
  "name": "vine-console",
  "description": "Voxel-based 9-trit fantasy console",
  "author": "Alex Bates (https://imalex.xyz)",
  "license": "0BSD",
  "dependencies": {
    "three": "^0.111.0"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "prettier": "^1.19.1",
    "typescript": "^3.7.3"
  },
  "scripts": {
    "dev": "parcel index.html",
    "build": "parcel build index.html",
    "pretty": "prettier --write **/*.* index.html --no-semi --trailing-comma all --single-quote"
  }
}
